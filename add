#!/bin/bash
# Обертка для добавления сервера в мониторинг одной командой
# С проверкой актуальности скрипта

if [ $# -lt 2 ]; then
    echo "Использование: curl -fsSL https://raw.githubusercontent.com/Morningstar2808/server-monitoring-scripts/master/add | bash -s SERVER_NAME TAILSCALE_IP [ANGIE_PORT] [CADVISOR_PORT]"
    echo "Пример: curl -fsSL https://raw.githubusercontent.com/Morningstar2808/server-monitoring-scripts/master/add | bash -s \"web-server-01\" \"100.87.187.88\" \"8081\" \"8080\""
    echo "  - Если порт не нужен, укажите пустую строку: \"\""
    exit 1
fi

SERVER_NAME="$1"
TAILSCALE_IP="$2"
ANGIE_PORT="${3:-}"
CADVISOR_PORT="${4:-}"

echo "=== Добавление сервера в мониторинг ==="
echo "Сервер: $SERVER_NAME"
echo "IP: $TAILSCALE_IP"
if [ -n "$ANGIE_PORT" ]; then echo "Angie порт: $ANGIE_PORT"; fi
if [ -n "$CADVISOR_PORT" ]; then echo "cAdvisor порт: $CADVISOR_PORT"; fi
echo ""

SCRIPT_NAME="add_server_to_monitoring.sh"
SCRIPT_URL="https://raw.githubusercontent.com/Morningstar2808/server-monitoring-scripts/master/$SCRIPT_NAME"
NEED_DOWNLOAD=true

# Проверяем, есть ли уже скрипт и актуальный ли он
if [ -f "$SCRIPT_NAME" ]; then
    echo "📄 Найден локальный скрипт $SCRIPT_NAME"
    
    # Проверяем возраст файла (если старше 1 дня - обновляем)
    if [ $(find "$SCRIPT_NAME" -mtime -1 2>/dev/null | wc -l) -gt 0 ]; then
        echo "✅ Скрипт актуальный (обновлен менее суток назад)"
        NEED_DOWNLOAD=false
    else
        echo "⚠️  Скрипт устарел, обновляем..."
    fi
fi

# Скачиваем скрипт если нужно
if [ "$NEED_DOWNLOAD" = true ]; then
    echo "⬇️  Скачиваем актуальный скрипт..."
    if ! wget -q "$SCRIPT_URL" -O "$SCRIPT_NAME"; then
        echo "❌ Ошибка скачивания скрипта"
        exit 1
    fi
    chmod +x "$SCRIPT_NAME"
    echo "✅ Скрипт обновлен"
fi

echo ""
echo "🚀 Запускаем добавление сервера..."

# Запускаем с параметрами (теперь с cadvisor_port)
./"$SCRIPT_NAME" "$SERVER_NAME" "$TAILSCALE_IP" "$ANGIE_PORT" "$CADVISOR_PORT"

echo ""
echo "✅ Готово!"
